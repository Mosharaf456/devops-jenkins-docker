all:
  vars:
    ansible_connection: ssh

test:
  hosts:
    test1:
      ansible_host: client1_remote
      ansible_user: root
      ansible_private_key_file: /tmp/ansible/remote-key
      ansible_python_interpreter: /usr/bin/python3


