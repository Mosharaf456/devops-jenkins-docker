What is ansible roles? 
In simple word: 
Ansible role are a way to group multiple tasks together into one container to do the automation 
in a very effective manner with clean and directory structure.
Whenever we need to do big taks 
that have multiple tasks , multiple playbooks are there , it is difficult to manage in a single file .

To do proper way , organized way : 
we always go with the roles. 

roles allow: 
- to create minimal playbooks 
- that then look to a directory structure to determine actual configuration steps that need to perform.

roles are set of tasks and additional file for certain role which allow you to break up the configurations
and you also easily reuse the codes by anyone it the role is suitable to someone.

Organizing things into roles allows you to reuse the common configuration steps between different types.

TO create a Ansible roles, use ansible-galaxy command which has the template to create it. 
offline - create offline mode rather than getting from online repository 

> ansible-galaxy init /etc/ansible/roles/apache --offline 

// for online 
> ansible-galaxy init /etc/ansible/roles/apache

--apache was created successfully 

Where , ansible-galaxy is the command to create the role using the template 
Init is to initialize the role. 
apache is the name of the role 
offline - create offline mode rather than getting from online repository .


root@mosharaf-desktop:/etc/ansible# ansible-galaxy init roles/facts_var --offline
- Role roles/apache was created successfully
root@mosharaf-desktop:/etc/ansible/roles# tree
.
├── apache
│   ├── defaults
│   │   └── main.yml
│   ├── files
│   ├── handlers
│   │   └── main.yml
│   ├── meta
│   │   └── main.yml
│   ├── README.md
│   ├── tasks
│   │   └── main.yml
│   ├── templates
│   ├── tests
│   │   ├── inventory
│   │   └── test.yml
│   └── vars
│       └── main.yml
└── ssh_service
    ├── defaults
    │   └── main.yml
    ├── files
    ├── handlers
    │   └── main.yml
    ├── meta
    ├── README.md
    ├── tasks
    │   └── main.yml
    ├── templates
    └── vars
        └── main.yml


vars is a variable 

default for default variable declare 

vars> default 
files that need to transfer to client machine 
templates - like config template 

meta to store author like which patient 

main file is tasks 



> ansible-galaxy role init my_local_role

> ansible-galaxy init /etc/ansible/roles/project1 --offline 

for online
> ansible-galaxy init /etc/ansible/roles/apache

Run the Playbook with a Custom Role Path

Since the role is not in /etc/ansible/roles/, tell Ansible where to find it:

cd ~/my_ansible_project
ansible-playbook -i inventory site.yml --roles-path=roles/

Alternatively, you can define the role path in ansible.cfg:
[defaults]
roles_path = ~/my_ansible_project/roles

//then simply run 
ansible-playbook -i inventory site.yml

Key Takeaways:
    You don’t have to place roles in /etc/ansible/roles/.
    Use the --roles-path option or ansible.cfg to specify custom paths.
    The playbook will still run normally as long as it can locate the roles.


ansible-playbook -i $ANSIBLE_TRAINING_HOSTS_PATH $ANSIBLE_CON_TEST_PLAYBOOK_PATH --limit dev


ansible-galaxy role init "/media/btl-671/915d77f5-a895-4107-aadf-337891150b1343/1.GIT REPOS/1.git-devops/cloud-computing-kb/AGCGPlaybook/AGCG_Automation/roles/backup" -v --force 

ansible-galaxy role init "/media/btl-671/915d77f5-a895-4107-aadf-337891150b1343/1.GIT REPOS/1.git-devops/cloud-computing-kb/AGCGPlaybook/AGCG_Automation/roles/build" -v --force

ansible-galaxy role init "/media/btl-671/915d77f5-a895-4107-aadf-337891150b1343/1.GIT REPOS/1.git-devops/cloud-computing-kb/AGCGPlaybook/AGCG_Automation/roles/deploy" -v --force 

ansible-galaxy role init "/media/btl-671/915d77f5-a895-4107-aadf-337891150b1343/1.GIT REPOS/1.git-devops/cloud-computing-kb/AGCGPlaybook/AGCG_Automation/roles/rollback" -v --force


ansible-galaxy role init "/var/jenkins_home/devsecops/roles/backup" -v --force 


