****Modules***
Modules are programs that ansible used to perform operations on managed nodes / hosts . 
They are ready to use tools designed to perform specific opeartions. 
Modules can be executed from the ansible commandline  or used in ansible playbook s to execute tasks. 

When run, modules are copied to the manged host and execute there.

Ansible comes packaged with over 500 modules available for use. These prepackaged modules can be 
used to perform a wide range of tasks, such as cloud, user, package and service management.

There are three types of ansible modules:
1. Core modules - insluded with ansible by installing by ansible installtion and manged by ansible dev team.
It is the most important modules and used for administritive tasks.

2. Extra modules -  are currently included with Ansible but shift seperate in future.
maintained by the community , not by ansible team. for manging newer technology such as OpenStack.

3. Custom modules - are modules devloped by end users and not shifted by Ansible.  If a module does not
already exist for a task, an administrator can write a new module to implement it.-- using python language 

Custom modules location : 
$ANSIBLE_LIBRARY environment variable 

#library = /usr/share/my_modules

for Red Hat /usr/lib/pythonx.x/site-packages/ansible/modules directory



## Modules categories:: 
cloud, packaging,utilities,clustering,network

files,soucre control , web interface, inventory , commands 

notification, system, windows, messaging, monitoring, database.

************for details of modules: ***********
website: docs.ansible.com 

>> ansible-doc apt
 MODULE ansible.builtin.apt (/usr/lib/python3/dist-packages/ansible/modules/apt.py)

location: /usr/lib/python3/dist-packages/ansible/modules/apt.py

search packages :
> dpkg -l | grep -iw tree
> apt-cache search -n ^tree$
// manually installation 
> apt update && apt install -y tree

1. Show Enabled Repositories
> apt-cache policy

2. 4. Use apt-add-repository (If Available)
> apt-add-repository --list

3. Check Repository Files in /etc/apt/sources.list.d/
> ls -l /etc/apt/sources.list.d/
To see the contents of a specific repository file:
> cat /etc/apt/sources.list.d/*.list

# install package 
>ansible -m apt -a "name=tree state=present" dev
> dpkg -l | grep -iw tree

## remove package 
>ansible -m apt -a "name=tree state=absent" dev
> dpkg -l | grep -iw tree

## apache2
ansible -m apt -a "name=apache2 state=present" dev

ansible -m apt -a "name=apache2 state=absent" dev

# service apache2 start
>ansible -m service -a "name=apache2 state=started" dev

> ansible -m service -a "name=apache2 state=started" dev

> ansible all -m command -a "service apache2 start" --become -K

> ansible all -m command -a "apache2ctl -D FOREGROUND" --become -K


## nginx
ansible all -m service -a "name=nginx state=stopped" --become -K
ansible all -m command -a "nginx -s stop" --become -K
ansible all -m command -a "service nginx stop" --become -K

###To Verify Nginx is Stopped
ansible all -m shell -a "ps aux | grep nginx" --become -K
ansible all -m shell -a "netstat -tulnp | grep :80" --become -K



## enabled a service 
ansible all -m systemd -a "name=apache2 enabled=yes" --become -K

> ansible all -m service -a "name=apache2 enabled=yes state=started" --become -K 

ansible all -m shell -a "service apache2 status" --become -K

ansible all -m shell -a "systemctl is-enabled apache2" --become -K


// Write playbook strategy
when possible , try to avoid the command,shell and raw modules in playbooks beacuse these take arbitrary commands,
it is very easy to write non-idempotent playbooks with these modules.


non-idempotent vs idempotent 
## playbook
--- 
- name: Non-idempotent approach with shell module 
  shell: echo "nameserver 192.168.1.101" > /etc/resolv.conf 


--- 
- name: Non-idempotent approach with shell module 
  shell: echo "nameserver 192.168.1.101" > /etc/resolv.conf 


same content puted if runs multiple times, not checked available or not. Same task does again and again this is called 
non-idempotent behaviour.


## idempotent
instead of shell module use copy module for idempotent 

- name: idempotent approach with copy module 
  copy: 
    dest:/etc/resolv.conf
    content:"nameserver 192.168.1.101\n"


If theere already existence , then can easily test to see if the state has already been met, if it has will make no changes.


Idempotent playbooks can be run repeatedly to ensure systems are in a particular state 
without disrupting those systems if they already are. 


#### lab session behaviour idempotent  and non-idempotent 

> ansible-playbook 5.non-idempotent.yml 


##check on client machine 
date
Sun Feb 23 09:08:09 UTC 2025
root@5c305aa56ecf:/tmp# ls -ltrh resolve.conf 
-rw-r--r-- 1 root root 23 Feb 23 09:08 resolve.conf
root@5c305aa56ecf:/tmp# 

##### idempotent 
> ansible-playbook 6.idempotent.yml

root@mosharaf-desktop:/etc/ansible# cat 6.idempotent.yml
---
- hosts: dev
  become: true
  become_user: root
  tasks: 
    - name: Idempotent approach with shell module 
      copy:
        dest: /tmp/resolv1.conf
        content: "nameserver 192.168.1.102\n"










