# make new item 
# freestyle project

# Build steps
# Execute shell 

touch /tmp/hello
echo  "Hello World" > /tmp/hello
ls -al /tmp 
cat /tmp/hello
echo "current date is $(date)"
NAME=MOSHARAF
echo "HELLO , $NAME. current and date is $(date)"
echo "HELLO , $NAME. current and date is $(date)" > /tmp/info
cat /tmp/info


Build Steps
Execute shell script on remote host using ssh
Command
NAME=Mosharaf
echo "Hello $NAME, Current date and time is $(date)" > /tmp/remote-info
