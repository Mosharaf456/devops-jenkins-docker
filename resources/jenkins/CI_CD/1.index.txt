CI/CD is nothing else but a methodology/strategy to deploy code faster to production! 

The process is defined by some steps, starting at CI which is Continuous Integration, where you build an test your code; optionally, you could pass to Continuous Delivery, which deploys your built and tested app to a dev/stg/qa env (just to test again) and finally you deploy to production

1. CODE -> Continuous Integration (Commit --> Unit,Integration,functional testing)

2. Continuous Delivery -> 
Deploy to testing Env 
-> Acceptance Testing 

3. Continuous Deployment -> Deploy /Promote to production

###################

Continuous Integration is nothing but building and testing our application.

Continuous Delivery is optional , nothing but app acceptance testing , normally deploy to our test 
environment.

Continuous Deployment is final steps : is to time deploy to production


###########
It,s nothing else a strategy that allows us to automate the code Delivery,
reducing errors and reducing manual human effort.

jenkins pipeline, normally known as jenkins as code, pipeline as code.

stages means step of our workflow.

Jenkins, we have got two types of pipeline.
1. Declarative pipeline
2. Scripted pipeline

1.
normally Declarative pipeline is well, who don,t know 
about programming , normally SysAdmins or people that do not develop the applications.

Jenkinsfile template down below: 

***********Jenkinsfile (Declarative pipeline)************
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //
            }
        }
        stage('Test') {
            steps {
                //
            }
        }
        stage('Deploy') {
            steps {
                //
            }
        }
    }
}



2.
Scripted pipelineis is actually written on Groovy language.
You need to define every single thing in grooovy as something like java.
You need to learn groovy and java.

Jenkinsfile template down below: 
******* Jenkinsfile (Scripted pipeline )*********
node {
    stage('Build') {
        //
    }
    stage('Test') {
        //
    }
    stage('Deploy') {
        //
    }
}


Both of this pipelines work for same thing.  

So, we gonna talk about Declarative pipeline.
Simple to use.
inside the stages -> steps-> 
we can do whatever we want using any programming language.
Or any bash or Ansible or Python.

Install jenkins piepline plugins:
search this:
pipeline

###########create our first pipeline ----

go to jenkins-data folder 
/var/jenkins_home


find by default recursivly do searching...
For example, this will search for any file or directory named data recursively from the current directory (.):
> find . -name "data"
If you want to search only for directories named data:
> find . -type f -name "data"
If you want to search only for files named data:
> find . -type d -name "data"


pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "building..."
            }
        }
        stage('Test') {
            steps {
                echo "Testing..."
            }
        }
        stage('Deploy') {
            steps {
                echo "deploying..."
            }
        }
    }
}

agent any is nothing how pipeline will be executed, 
is it docker or another source.

Any available agent will going to build this pipeline.

You can declare as much as stages , you want.

steps inside -- Normally ths accept bash
, you can do like bash commands.
In this steps section you can all of this magic,
executing script.



stage('Build') {
    steps {
        echo "Normally ths accept bash"
    }
}




///////Now from UI of jenkins
create a new item , it will pipeline project selction
Pipeline
Define your Pipeline using Groovy directly or pull it from source control.


////Add multistage to your pipeline 
pipeline {
    agent any 
    stages {
        stage ('Build') {
            steps {
                sh 'echo "My first multistage pipeline"'
                sh '''
                    echo "By the way can do more stuff here"
                    ls -lah
                '''
            }
        }
    }
}


////////////////retry::: stages Timeout as for reference

pipeline {
    agent any 
    stages {
        stage ('Timeout') {
            steps {
                retry(3) {
                    sh 'I am not going to work'
                }
            }
        }
    }
}


///////name of the stage can be define anything you want. 

pipeline {
    agent any 
    stages {
        stage ('Deploy') {
            steps {
                retry(3) {
                    sh 'echo Hello'
                }
                timeout(time: 3, unit: 'SECONDS') {
                    sh 'sleep 5'
                }
            }
        }
    }
}

///////////// environment variable 

pipeline {
    agent any 

    environment {
        NAME = 'Mosharaf'
        LAST = "Hossain"
    }
    
    stages {
        stage ('Building pipeline') {
            steps {
                sh 'echo $NAME $LAST'
            }
        }
    }
}

///////////Credential on piepline
// built in defined credentials function 
// TEST will be the name of defined credentials, can be any as of cred inside

pipeline {
    agent any 

    environment {
        secret = credentials('TEST')
    }
    
    stages {
        stage ('Example stage 1') {
            steps {
                sh 'echo $secret'
            }
        }
    }
}



//// post actions on Jenkinsfile
// unstable means job was failing and now recovered..
/// echo 0 means success and 1 means fails in bash script.
// here exit 1  means fails.


/// for fail action 

pipeline {
    agent any 
    
    stages {
        stage ('Test') {
            steps {
                sh 'echo "Fail!"; exit 1'
            }
        }
    }

    post  {
        always {
            echo 'I will always get executed :D'
        }
        success {
            echo 'I will only get executed, if it success'
        }
        failure {
            echo 'I will only get executed, if it fails'
        }
        unstable {
            echo 'I will not executed, if not stable'
        }
    }
}


/// for success

pipeline {
    agent any 
    
    stages {
        stage ('Test') {
            steps {
                sh 'echo "success!"'
            }
        }
    }

    post  {
        always {
            echo 'I will always get executed :D'
        }
        success {
            echo 'I will only get executed, if it success'
        }
        failure {
            echo 'I will only get executed, if it fails'
        }
        unstable {
            echo 'I will not executed, if not stable'
        }
    }
}

